set = "ensembl-webteam/core"
name = "v_2"
version = 1
description = """

Show variant summary track. Perhaps surprisingly, basically a wiggle track where colours are
used rather than heights.

Uses data sequences from "v_2":
- values
- range

"""


[setting.v_2_name]
default = false
description = """

boolean: if true show track label.

"""
