import "lib:std"; use "std";
import "lib:peregrine"; use "peregrine";

/* most zoomed out gene view: no structure, but with labels */
p := lane_new("self()","gene",19,50,6);
lane_add_tag(p,"local");
lane_add_trigger(p,["track","gene-pc-fwd"]);
lane_apply(p);
